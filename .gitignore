node_modules/
dist/
build/
out/
cache/
broadcast/
artifacts/
typechain-types/
coverage/
.turbo/
.next/

# Local data
data/
dataset/
*.sqlite
*.sqlite-journal
*.sqlite-shm
*.sqlite-wal
*.db
*.db-journal

# Env / secrets
.env
.env.local
.env.*.local
ops/secrets/
.ssh-keys/
id_ed25519
*.pem
*.key
*.seed
*.token
*.secret
*private-key*
*wallet*.json

# Local working handoff/findings notes — never tracked.
# The cortex repo is the canonical public CoreTex client; per-session handoff
# docs are workspace scratch and stay local.
docs/HANDOFFS/

# Local model/cache material; commit manifests/hashes instead unless release policy approves.
models/
model-cache/
.cache/
*.safetensors
*.gguf
*.onnx
*.bin

# Logs
*.log
logs/
npm-debug.log*
pnpm-debug.log*

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Claude Code / agent harness scratch
.claude/

# Foundry
broadcast/*/dry-run/
cache/forge/
cache/foundry/
# foundry.lock — submodule revs are pinned in .gitmodules; the lock file is a
# per-host build artifact and must not enter the canonical CoreTex tree.
contracts/foundry.lock
foundry.lock

# pnpm / npm
.pnpm-store/
package-lock.json.bak

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.pytest_cache/
.mypy_cache/
*.tsbuildinfo
.venv/
venv/
.python-version

# Local/generated experiment scratch outputs
experiments/results/phase7-real-smoke/
experiments/results/synthetic-dryrun/winner.json

# Generated DACR corpus (regenerated from HF dataset by scripts/build-corpus-from-dacr.mjs)
benchmark/fixtures/dacr-v0/

# Ephemeral SSH keypair for one-off Vast.ai calibration host (regenerated per rental)
.ssh-vast-*
release/calibration/a100-2026-05-15/*.log
